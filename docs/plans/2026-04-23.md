- Introduce Sidebar: https://ui.shadcn.com/blocks/sidebar , with the following:
    - Dashboard (analytics charts with filters based on https://ui.shadcn.com/charts/area )
    - Projects
    - Assets
    - Results
- Shared system prompt is not editable by the user, it's ours! And the rest too. Only have 'personalization' be editable. The rest might be variable and dynamically injected, but it's not editable by the user
- We shouldn't have JSON-based fields; it should be for non-technical people
- We shouldn't be able to click 'Run tests' multiple times
- Visual Diffs for deterministic changes
- Golden paths + perturbation tests
- 'Run tests' should have clear what to do before running tests while the button is deactivated